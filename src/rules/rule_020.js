// src/rules/rule_020.js
module.exports = {
  id: 'rule_020',
  name: 'Account activity heuristic (placeholder)',
  defaultEnabled: true,
  async check(context) {
    // Placeholder: combine signals in the future
    return { action: 'none' };
  }
};
